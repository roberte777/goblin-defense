-- Game configuration
local config = {
    -- Virtual resolution (what we design for)
    virtual_width = 1280,
    virtual_height = 720,
    
    -- Current scale factors
    scale_x = 1,
    scale_y = 1,
    
    -- Offset for centering the game
    offset_x = 0,
    offset_y = 0
}

return config 